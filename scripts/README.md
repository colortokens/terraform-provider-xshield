# Maintaining the SDK and OpenAPI document

`xshield_oas3.yaml` and everything under `internal/sdk` are edited directly. Files still carrying a
`Code generated by ... DO NOT EDIT.` header are hand-maintained despite the header.

`xshield_oas3.yaml` is a hand-kept OpenAPI 3 description of the parts of the platform API the
provider uses. It is a convenience, not an authority: when it disagrees with what the API actually
accepts, the API wins and the document needs correcting.

## Refreshing against a platform release

1. **Diff the routes.** Compare the paths in `xshield_oas3.yaml` against the API's current route
   list. Routes do disappear between releases: the port and path approval endpoints were removed,
   and `refresh-progressive` was later renamed to `refresh-assetpolicy`.
2. **Update `xshield_oas3.yaml`.** Bring the changed definitions across. If you are translating from
   a Swagger 2 description, `#/definitions/model.X` becomes `#/components/schemas/X`, a `body`
   parameter becomes a `requestBody`, and `type` on a non-body parameter moves under `schema`.
3. **Update `internal/sdk`.** Each operation is one function on a service in `internal/sdk/*.go`,
   with a request/response pair in `internal/sdk/models/operations` and any payload types in
   `internal/sdk/models/shared`. Copy the closest existing operation and change the method, URL,
   request body type and status-code cases. A new service needs a `newX` constructor and a field on
   `Xshield` in `internal/sdk/xshield.go`.
4. **Regenerate the asset model.** Run `python3 scripts/gen_asset_provider.py .` after changing
   `shared.AssetDetails`. It rewrites the model struct, schema and refresh mapping for both the asset
   resource and its data source so every declared attribute is one the API actually returns. The
   script is idempotent and leaves CRUD code untouched.

   The data source gets every field. The **resource** gets only the ones in `RESOURCE_FIELDS`,
   because it is import-only: `Create` is refused outright and `Update` sends just those fields plus
   the enforcement toggles. Everything else in `AssetDetails` is agent-reported or platform-derived,
   so it moves with no Terraform action behind it; holding it in resource state converges on nothing
   and makes every refresh report drift. A new field is therefore data-source-only unless the
   resource actually writes it. `TestAssetResourceHoldsOnlyTheManagedSurface` fails if that set
   widens by accident. Import statements in the generated `*_sdk.go` files are derived from the
   emitted body, so they stay correct as the set changes.
5. **Format, build, test.** `gofmt -w internal && go build ./... && go test ./internal/...`.

## Things the tests pin down

`internal/sdk/sdk_test.go` asserts what goes on the wire, because a mismatch there fails only
against a live tenant. It covers the zero-trust body (`inboundToDeploymentState`, not the removed
`inboundToState`), the port deployment plan, the renamed asset-policy refresh path, and the fact
that a 304 carries no body. `internal/provider/asset_mapping_test.go` decodes a realistic asset
payload and asserts the enforcement fields reach Terraform state.

Add to these whenever you touch an operation. A test that only proves the code compiles is not worth
the line count.

## Where the document has been wrong before

Each of these was a real defect, not a formatting nit, and each cost real debugging time:

- **`SearchInput` nests its paging.** `limit`, `offset` and `sort` belong inside a `pagination`
  object. They sat at the top level, where the API ignores them, so every list ran with the default
  page of 100 rows and no sort, silently.
- **`GET /api/fields` answers with `columns`.** The document described a different shape, so the
  decode produced an empty catalogue from a perfectly good response.
- **Template deduct answers 202, not the 204 the document listed.**
- **`SortOrder` is the string `Asc` or `Desc`,** not the integer enum the document described.

When a response looks empty or a parameter looks ignored, check what the API actually does before
believing the document.

## Known gaps

- **`edrInfo` is deliberately absent** from `AssetDetails`. The API returns it; the provider drops it
  rather than modelling the nested EDR host-import rules. Unknown JSON fields decode harmlessly.
- **Documentation generation needs a newer toolchain.** `go generate ./...` runs `tfplugindocs`,
  which downloads a Terraform CLI and fails signature verification with `openpgp: key expired`. Only
  `terraform-plugin-docs` v0.25.0 fixes it, and that version raises the module's `go` directive to
  1.25.8. The docs in this tree were produced by bumping to v0.25.0, generating, and reverting the
  dependency, so no toolchain bump has landed. Either bump it deliberately or put a Terraform CLI on
  `PATH`, which `tfplugindocs` prefers over downloading one.

  Every attribute description lives on its schema, so a regeneration loses nothing. Keep it that
  way: edit the `Description` on the attribute, never `docs/*.md`.
